spring:
  config:
    import: configserver:http://CONFIG-SERVICE
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: CONFIG-SERVICE
#      enabled: true
#      uri: http://config-service
